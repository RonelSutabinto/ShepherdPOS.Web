@*==================================================================================================================================
===Some part of code is an autogenerated razor page code from asp.net core blazor framework that I added mudblazor component========
====================================================================================================================================
I will used this razor page to handle landing page index. I add some important part of app functions================================
===================================================================================================================================*@


@inherits LayoutComponentBase


<MudThemeProvider Theme="customTheme" />

<MudLayout>
    <MudAppBar Elevation="1" Color="Color.Primary">
        <MudImage Src="/Images/Logo_w.png" Width="50" Alt="Logo" Class="rounded-lg" />
        <MudText Typo="Typo.h6" Class="ml-2">Shepherd POS</MudText>
        <MudToolBar DisableGutters="true">
            <MudIconButton Icon="@Icons.Material.Outlined.Menu" Color="Color.Inherit" OnClick="@((e) => DrawerToggle())" Class="ml-3" />
        </MudToolBar>

        <MudSpacer />
        <MudAvatar Color="Color.Dark" Edge="Edge.End">R</MudAvatar>
    </MudAppBar>

    <MudDrawer @bind-Open="_drawerOpen" ClipMode="DrawerClipMode.Always" Elevation="2">
        @*NavMenu*@
        <NavMenu />
    </MudDrawer>
    <MudMainContent>
        @*Body*@
        <MudContainer MaxWidth="MaxWidth.Large">
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>

@code {
    private ErrorBoundary? errorBoundary;
    bool _drawerOpen = true;

    protected override void OnParametersSet()
    {
        errorBoundary?.Recover();
    }

    MudTheme customTheme = new MudTheme()
    {
        Palette = new Palette()
        {
            Primary = Colors.Green.Default,
            Secondary = Colors.Green.Accent3,
            AppbarBackground = Colors.Teal.Accent4,
            Tertiary = Colors.Teal.Accent1,
        },

        Typography = new Typography()
        {
            H6 = new H6()
            {
                FontFamily = new[] { "Roboto", "Helvetica", "Arial", "sans-serif" },
                FontSize = "1.25rem",
                FontWeight = 600,
                //LineHeight = 1.6,
                //LetterSpacing = ".0075em"
            },

            H5 = new H5()
            {
                FontFamily = new[] { "Roboto", "Helvetica", "Arial", "sans-serif" },
                FontSize = "1.65rem",
                FontWeight = 500,
            },

            H4 = new H4()
            {
                FontFamily = new[] { "Roboto", "Helvetica", "Arial", "sans-serif" },
                FontSize = "2.05rem",
                FontWeight = 400,
            },

            H3 = new H3()
            {
                FontFamily = new[] { "Roboto", "Helvetica", "Arial", "sans-serif" },
                FontSize = "2.85rem",
                FontWeight = 500,
            },

            H2 = new H2()
            {
                FontFamily = new[] { "Roboto", "Helvetica", "Arial", "sans-serif" },
                FontSize = "3.60rem",
                FontWeight = 350,
            },

            H1 = new H1()
            {
                FontFamily = new[] { "Roboto", "Helvetica", "Arial", "sans-serif" },
                FontSize = "4.95rem",
                FontWeight = 350,
            },
        },

    };



    void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }



}



@*

    <div class="page">
        <div class="sidebar">
            <NavMenu />
        </div>

        <main>
            <div class="top-row px-4 auth">
                <LoginDisplay />
                <a href="https://docs.microsoft.com/aspnet/" target="_blank">About</a>
            </div>

            <article class="content px-4">
                @Body
            </article>
        </main>
    </div>*@
